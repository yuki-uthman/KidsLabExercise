

describe('2. minus two numbers', function () {
  it('exercise2(1, 1) === 0', function () {
    let myCode = exercise2(1, 1)
    let answer = 0
    check(myCode, answer)
  });

  it('exercise2(2, 1) === 1', function () {
    let myCode = exercise2(2, 1)
    let answer = 1
    check(myCode, answer)
  });

});


