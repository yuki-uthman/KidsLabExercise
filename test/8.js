

describe('8. calculate the remainder', function () {
  it('exercise8(10, 3) === 1', function () {
    let myCode = exercise8(10, 3)
    let answer = answer8(10, 3)
    check(myCode, answer)
  });

  it('exercise8(12, 5) === 2', function () {
    let myCode = exercise8(12, 5)
    let answer = answer8(12, 5)
    check(myCode, answer)
  });

});
