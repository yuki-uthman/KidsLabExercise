

describe('6. minus 1 from the number', function () {
  it('exercise6(1) === 0', function () {
    let myCode = exercise6(1)
    let answer = answer6(1)
    check(myCode, answer)
  });

  it('exercise6(10) === 9', function () {
    let myCode = exercise6(10)
    let answer = answer6(10)
    check(myCode, answer)
  });

});
