

describe('3. multiply two numbers', function () {
  it('exercise3(1, 1) === 1', function () {
    let myCode = exercise3(1, 1)
    let answer = 1
    check(myCode, answer)
  });

  it('exercise3(2, 5) === 10', function () {
    let myCode = exercise3(2, 5)
    let answer = 10
    check(myCode, answer)
  });

});
